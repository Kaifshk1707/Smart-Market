import { Platform } from "react-native";

export const Is_Adnroid = Platform.OS === "android";
export const Is_IOS = Platform.OS === "ios";

export const taxes = 15;
export const shippingFees = 10;